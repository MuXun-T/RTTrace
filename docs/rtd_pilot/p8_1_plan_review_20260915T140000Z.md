# RTD-PILOT P8.1 Plan Review

Status: PLAN_REVIEW_APPROVED

The read-only sub-agent plan was reviewed by the primary agent and approved
before implementation. It covers commit/tree and SHA-256 verification for the
P5, P7 and P8 frozen inputs; the P5 primary cohort (21 registry cases, 63
captures, 63 CaptureEpisodes, 12 positive and 9 control); independent-only P8
supplement handling; registry-case split and capture-repeat rules; development,
validation and holdout truth boundaries; permitted frozen inputs; frozen-output
integrity; baseline/ablation runnable/unavailable/failed states; case-clustered
estimands and confidence intervals; explicit invalid/failed/non-manifested
retention; table/figure provenance; immutable manifests; reproducibility;
risks, stop conditions and the minimum complete subset.

The plan records that the existing P7 method outputs are digest-only derived
outputs. No diagnostic prediction or truth is inferred from them. Any metric
without a frozen, contract-compatible prediction-to-truth mapping is reported
as unavailable, and no holdout truth is used for selection or tuning.

Frozen inputs remain read-only. All implementation artifacts will be created
append-only under a new unique P8.1 directory.
