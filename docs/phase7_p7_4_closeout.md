# P7.4 Corrective Closeout

This corrective closeout starts at `ca31b6c`, which was an unsuccessful final-closeout attempt, not a freeze. Its final audit found that expected/profile comparison was not in the replay decision path, `comparison_attempted` remained false for apparent passes, reference-only reports were absent, and a complete Python-regression exit result had not been captured.

P7.4 began at `42b651f`. Its preserved original commits are `8d02c1e`, `20ed9f2`, `f1599b8`, `ed6c2f5`, `5dd3ab8`, `1e62f58`, `75a72c2`, `8b17360`, `0bdcae0`, and `ca31b6c`. Corrective implementation is `0155418` (`phase7: complete semantic replay corrective validation`); this closeout commit is the independent final freeze. No original commit is rewritten, amended, or squashed.

## Corrective Facts

- The four frozen expected fixtures are manual raw-trace audits, bind the raw trace SHA-256 and the exact `p7.4-freertos-semantic-exact-v1` profile, and are not generated by the replay implementation.
- A formal `replay_pass` requires replay attempted plus comparison attempted, completed, and matched, with trace, expected, and actual identities present.
- Zephyr and Zephelin reference-only reports verify the immutable P7.2 inventory SHA-256 `ad15a481e2dde8eea0ef2b6e3feecc083e30699532296f27d1095cacaedde54b` and their exact acquisition status before reporting reference-only.
- `example.vcd` contains an `x` scalar at physical line 15440. The frozen VCD contract requires this form to fail closed, so it is a formal `replay_fail` with `UNSUPPORTED_REQUIRED_RECORD`; it is not reference-only and comparison is not invoked.
- `example-50k.btf` retains its source-order regression: source record 2037, physical line 2042, `26113 -> 26112`, CPU `null`. Comparison completes and matches the manually audited summary, but the invariant makes the final state `replay_fail/TIMESTAMP_REGRESSION`.

## Frozen Inputs

P6.4 canonical SHA-256 remains `fe6bb93191fc72e9584339878c0418b1005f3631f0e213e92b8dcbeb2bd2811e`. The immutable P7.3 opened report remains `dd835f9a8595cd46d61ab1c3e05c6afa6473e4d38402ebacef82ded804ddbef1`.

The four raw SHA-256 values remain `571183cbafba85f0eeb9c74cf2350f02a4e628abe514409dd3c1b68286969f44` (`example.btf`), `7aedcf4e14bb0e34838613225ff50e6cb8d76ed62e12bcfe16aa6101b0f36997` (`example.vcd`), `eb15beee65c62d53a3bbf9db5ebb36318156b720e4bd909272605dfeb1c6eed1` (`example-4cores.btf`), and `f032a6af43334abc5c5fbed6b145e711f0262e2b1dfa6d3a44a28c37b6308baa` (`example-50k.btf`). Source, package, raw trace, P6.4, P7.0-P7.3, provenance, and license mutations are zero in this corrective work.

## Validation Status

The final primary-agent run completed with exit 0 for P7.4 focused (`22 passed`, `3 subtests`), P7.3 package regression (`37 passed`, `46 subtests`), external validation (`55 passed`, `46 subtests`), schema mirror (`19 passed`, `17 subtests`), and full `tests/python` (`741 passed`, `999 subtests`, 133.60 seconds). Canonical CLI reproduction ran each of the six cases twice with byte and SHA-256 equality; exits were `0/4/0/4/2/2` in reproducibility-index order.

The final states are BTF 1-core and 4-core `replay_pass` with completed matched comparison; VCD `replay_fail/UNSUPPORTED_REQUIRED_RECORD` before comparator; BTF 50k `replay_fail/TIMESTAMP_REGRESSION` after completed matched comparison; and Zephyr/Zephelin `reference_only` with comparison not attempted. Source, package, and raw post-snapshots are revalidated on every started replay return path; all intact canonical cases report mutation counts of zero.

All canonical truth-path invocation counters are zero for LLM, Advisor, Feedback, network, shell, and subprocess. `hardware_validation=false`; `proof_parity=not_evaluated`; `proof_correctness=not_evaluated`; `diagnosis_correctness=not_evaluated`; P7.5 is not started.
